import java.util.HashMap;

// Через HashMap
HashMap<String, Integer> hashTable = new HashMap<>();
hashTable.put("key1", 100);
hashTable.put("key2", 200);
System.out.println(hashTable.get("key1")); // 100

// Собственная реализация через массив LinkedList
